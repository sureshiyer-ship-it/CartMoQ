# CartMoQ
MOQ Validation at Cart checkout

## Local preview

Open the login page from `web/login.html` in your browser. If you are serving files locally, a quick way is to run a static server:

```bash
python3 -m http.server 8080 --directory web
```

Then visit `http://localhost:8080/login.html`.
